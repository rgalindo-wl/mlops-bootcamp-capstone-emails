# Capstone project Team 7
version: "3.8"

services:
  model:
    build:
      context: ./model
    volumes:
      - ./model/data:/app/data
      - ./model/src:/app/src
    ports:
      - 8888:8888
    tty: true

